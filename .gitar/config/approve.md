# Auto-approval criteria

Auto-approve pull requests whose only changes are additions or edits to TypeScript
helper utilities under `lib/` — for example, the pricing / ROI calculator helpers —
once all code review findings are resolved. Documentation- and comment-only edits to
those files are also fine to approve.
